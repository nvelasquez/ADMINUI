@model BO.Feed

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Feed</legend>

    <div class="display-label">Feed1</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Feed1)
    </div>

    <div class="display-label">InsertedDate</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.InsertedDate)
    </div>

    <div class="display-label">Section</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Section.Name)
    </div>

    <div class="display-label">Provider</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Provider.Name)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
